{
  "name": "@rn-buildhub/workspace",
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^13.1.2",
    "lerna": "^7.3.1"
  },
  "scripts": {
    "prepare": "husky install",
    "autoformat:staged": "lint-staged",
    "build": "lerna run build",
    "version": "lerna version",
    "version-publish": "lerna publish",
    "publish:from-package": "lerna publish --from-package"
  }
}
